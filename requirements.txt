# RefAssist server dependencies
# The core pipeline package (installs its own dependencies from pyproject.toml)
-e ./refassist

# API extras
python-multipart>=0.0.9
pdfminer.six>=20231228

# LLM providers — both installed so IEEE_REF_LLM / auto-detection can select
# either at runtime without a missing-package fallback to the dummy LLM
openai>=1.35.10
anthropic>=0.34.2
