# RefAssist

Agentic IEEE Reference Pipeline using LangGraph — modular and production-ready.

- LLM adapter (OpenAI / Azure OpenAI / Anthropic / Ollama / Dummy)
- Async multi-source lookups (Crossref, OpenAlex, Semantic Scholar, PubMed, arXiv)
- Verification agents and bounded repair loop
- Outputs: IEEE string, CSL-JSON, BibTeX
- Mermaid graph render helper for notebooks
- Ready for both UI embedding and public API (FastAPI), plus a tiny CLI

## Install

```bash
pip install -e .
cp .env.example .env
